import React from 'react';

const SSELogo = ({ width = 200, height = 90, className = "" }) => {
    return (
       <img src="sgi.png" alt="sgi" width={width} height={height} className={className} />
    );
};

export default SSELogo;
